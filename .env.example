# Optional analytics. When blank, no Google Analytics script is requested.
# Production builds use `.env.production` (GA4: G-9KDDGC812M).
PUBLIC_GA_ID=

# Advertising is disabled by default. Enabling the slot does not configure a provider.
PUBLIC_ADS_ENABLED=false
